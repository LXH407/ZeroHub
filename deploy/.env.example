# ===== ZeroHub 环境变量示例（本地开发复制为 .env 放项目根，Render 在控制台 Environment Variables 填）=====

# 【必改！】JWT 签名密钥。生产环境务必设为 32+ 位随机字符串
# PowerShell 生成：-join ((48..57)+(65..90)+(97..122) | Get-Random -Count 32 | % {[char]$_})
JWT_SECRET=zerohub-secret-key-change-me-2026-pls

# 【可选】服务监听端口（Render 会自动注入 PORT=10000；本地默认 3000）
PORT=3000
